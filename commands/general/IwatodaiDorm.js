module.exports = {
    config: {
        name: "iwatodaidorm",
        aliases: [],
        description: "Plays iwatodai dorm",
        usage: "",
        category: "general"
    },
    run: async (client, message, args) => {
        let vc = message.guild.channels.find(c => c.name === "")
    }
}